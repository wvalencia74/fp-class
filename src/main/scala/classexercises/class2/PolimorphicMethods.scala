package classexercises.class2

object PolimorphicMethods {
  /*def foo(a:Int, b:Int) = ???

  def hof[A,  B](x: A, msg: String, f: A => B) = {
    s"el $msg es : ${f(x)}"
  }

  def dividir [A <: Double](dividendo:A, divisor:A):Double = {
    if(!divisor.eq(0)) dividendo/divisor else 0
  }

  def div:Double => Double => Double = x => y => y match {
    case 0 => 0
    case _ => x/y
  }*/
}
